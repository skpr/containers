schemaVersion: '2.0.0'

commandTests:
  - name:  "check for unloaded modules"
    command: "/usr/sbin/php-fpm"
    args: ["--test"]
    excludedError: [".*Unable to load dynamic library.*"]
  - name: 'check configuration'
    command: "sh"
    args: ["-c", "mkdir -p /data/app && php-fpm --test"]
